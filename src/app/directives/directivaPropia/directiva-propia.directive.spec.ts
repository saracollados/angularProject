import { DirectivaPropiaDirective } from './directiva-propia.directive';

describe('DirectivaPropiaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaPropiaDirective();
    expect(directive).toBeTruthy();
  });
});

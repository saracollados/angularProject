import { StatNamesPipe } from './stat-names.pipe';

describe('StatNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new StatNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
